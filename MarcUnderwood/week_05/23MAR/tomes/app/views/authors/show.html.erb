<h1><%= @author.name %></h1>
<%= image_tag(@author.image, :class => 'feature') %>
<ul>
  <li><%= @author.nationality %></li>
  <li><%= @author.dob.strftime '%A %e %B %Y' unless @author.dob.nil? %></li>
  <li><%= @author.genre %></li>
</ul>

<% @author.books.each do |book|  %>
  <%= link_to(image_tag(book.image, :class => 'thumb'), book) %>
<% end %>

<p>
  <%= link_to 'Edit author', edit_author_path(@author) %>
  <%= link_to 'Delete author', @author, :method => :delete %>
</p>
